// ignore_for_file: constant_identifier_names

enum AuthenticationLevel {
  NONE,
  BASIC,
  TOKEN,
}

enum Status {
  IDLE,
  LOADING,
  INITIALIZED,
  INSTALLED,
  COMPLETED,
  FAIL,
}

enum DataType {
  Boolean,
  Number,
  String,
  Array,
  Map,
  Date,
}
